extends layout 

block content
    for item in items
        .wrap   
            p 
                tr.odd(role='row')
                        | VENUE IS UNAVAILABLE 
                        br
                        br
                        | FROM: 
                        td #{item.lstart}
                        br
                        | TO:  
                        td #{item.lend}